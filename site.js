const toggle=document.getElementById('theme-toggle');let saved='light';try{saved=localStorage.getItem('rajeswari-theme')||'light'}catch{}function setTheme(theme){document.documentElement.dataset.theme=theme;toggle?.setAttribute('aria-label',theme==='dark'?'Switch to light theme':'Switch to dark theme');toggle?.setAttribute('title',theme==='dark'?'Switch to light theme':'Switch to dark theme');toggle?.setAttribute('aria-pressed',String(theme==='dark'))}setTheme(saved);toggle?.addEventListener('click',()=>{const next=document.documentElement.dataset.theme==='dark'?'light':'dark';setTheme(next);try{localStorage.setItem('rajeswari-theme',next)}catch{}});